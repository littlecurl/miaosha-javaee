<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mavendemo.dao.ItemStockDOMapper">

  <resultMap id="BaseResultMap" type="com.example.mavendemo.dataobject.ItemStockDO">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="stock" jdbcType="INTEGER" property="stock" />
    <result column="item_id" jdbcType="INTEGER" property="itemId" />
  </resultMap>

  <sql id="Base_Column_List">
    id, stock, item_id
  </sql>

  <!-- 手动修改的：主键自增 -->
  <insert id="insert" parameterType="com.example.mavendemo.dataobject.ItemStockDO" keyProperty="id" useGeneratedKeys="true">
    insert into item_stock (id, stock, item_id
      )
    values (#{id,jdbcType=INTEGER}, #{stock,jdbcType=INTEGER}, #{itemId,jdbcType=INTEGER}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.example.mavendemo.dataobject.ItemStockDO" keyProperty="id" useGeneratedKeys="true">
    insert into item_stock
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="stock != null">
        stock,
      </if>
      <if test="itemId != null">
        item_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="stock != null">
        #{stock,jdbcType=INTEGER},
      </if>
      <if test="itemId != null">
        #{itemId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from item_stock
    where id = #{id,jdbcType=INTEGER}
  </delete>

  <update id="updateByPrimaryKeySelective" parameterType="com.example.mavendemo.dataobject.ItemStockDO">
    update item_stock
    <set>
      <if test="stock != null">
        stock = #{stock,jdbcType=INTEGER},
      </if>
      <if test="itemId != null">
        item_id = #{itemId,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.example.mavendemo.dataobject.ItemStockDO">
    update item_stock
    set stock = #{stock,jdbcType=INTEGER},
      item_id = #{itemId,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>

  <!-- 扣减库存 -->
  <update id="decreaseStock">
    update item_stock
    <!--set stock = stock - #{amount}, 多了一个逗号引发的血案...   而且本句的注释不能用两个横杠，只能使用xml的注释-->
    set stock = stock - #{amount}
    where item_id = #{itemId} and stock >= #{amount}
  </update>

  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from item_stock
    where id = #{id,jdbcType=INTEGER}
  </select>
  <!-- 手动添加的方法 -->
  <select id="selectByItemId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from item_stock
    where item_id = #{itemId,jdbcType=INTEGER}
  </select>
</mapper>